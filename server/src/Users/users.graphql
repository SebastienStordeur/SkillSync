type Query {
  users: [User]
  user: User
}

type Mutation {
  signup(user: UserInput): User
}

type User {
  email: String!
  password: String!
}

input UserInput {
  email: String!
  password: String!
}
