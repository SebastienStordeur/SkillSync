type Query {
  getUsers: [User]
  login(user: UserInput): LoginResponse
  getUser(id: ID): User
}

type Mutation {
  signup(user: UserInput): User
}

type User {
  id: ID
  email: String!
  password: String
}

input UserInput {
  email: String!
  password: String!
}

type ID {
  token: String
}

type LoginResponse {
  success: Boolean
  message: String
  token: String
}
